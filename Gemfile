source 'https://rubygems.org'
gem 'jekyll', '~> 4.2.0'
gem 'webrick'
gem 'wdm', '>= 0.1.0' if Gem.win_platform?

group :jekyll_plugins do
  # If any of the above gems are Jekyll plugins, include them here as well
  gem 'jekyll-seo-tag'
  gem 'jekyll-paginate'
  gem 'jekyll-sitemap'
end